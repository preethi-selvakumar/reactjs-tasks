import React from 'react';

function AlertButton({ handleClick }) {
  return (
    <button className="alert-button" onClick={handleClick}>
      11. Click Me
    </button>
  );
}

export default AlertButton;
